# Custom for CodeBlocks. These files are checked out using LF locally
*.cbp       text eol=lf
*.workspace text eol=lf
*.wxs       text eol=lf

# Explicitly declare text files you want to always be normalized and converted
# to native line endings on checkout.
*.cpp text
*.h   text
*.rc  text
